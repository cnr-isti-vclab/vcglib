before_install:
  - sudo apt-get -qq update
  - sudo apt-get install cmake

# Enable C++ support
language: cpp

# Compiler selection
compiler:
  - clang
  - gcc

# Build steps
script:
  - cd apps/tridecimator
  - mkdir build
  - cd build
  - cmake ..
  - make
